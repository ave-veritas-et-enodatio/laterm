# laterm

Full contract, in precedence order: **SPEC.md** (what laterm must do to be laterm)
→ **ARCHITECTURE.md** (how this Rust implementation does it) → **AGENTS.md** (house
rules and project-specific traps).

This overrides the default single-contract-doc assumption: here AGENTS.md alone is
house rules, not the contract. Pass all three to dispatched agents.
